version: "2"
sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "queries.sql"
    gen:
      go:
        package: "postgres"
        out: "."
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true
